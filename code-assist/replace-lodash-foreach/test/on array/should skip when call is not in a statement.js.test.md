
## Input
```javascript input
const _ = require('lodash');

const input = [1, 2];
f(_.each(input, (value) => {
    console.log(value)
}));
```

## Configuration
```json configuration
{
  "extension": "js"
}
```
