
## Input
```javascript input
for (let i = elements.length - 1; i >= 0; i--) {
  const element = elements[i];
  console.log(i);
  console.log(element);
}
```

## Configuration
```json configuration
{
  "extension": "js"
}
```
