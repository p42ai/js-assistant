
## Input
```javascript input
const nx = elements.length;
for (const i = 0; i < n; ++i) {
    const element = elements[i];
    console.log(element);
    console.log("X");
}
```

## Configuration
```json configuration
{
  "extension": "js"
}
```
