
## Input
```javascript input
const _ = require('lodash');

const input = { a: 1, b: 2 };
const result = _.map(input, (value) => value * 2);
```

## Configuration
```json configuration
{
  "extension": "js"
}
```
