
## Input
```javascript input
if (x === 'a') {
    fa();
} else if (y === 'b') {
    fb();
} else if (x === 'c') {
    fc();
}
```

## Configuration
```json configuration
{
  "extension": "js",
  "selection": "0-0"
}
```
