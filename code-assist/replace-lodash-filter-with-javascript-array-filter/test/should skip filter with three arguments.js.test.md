
## Input
```javascript input
const _ = require('lodash');

const input = [1, 2];
const result = _.filter(input, (value) => value === 2, 'anotherArgument');
```

## Configuration
```json configuration
{
  "extension": "js"
}
```
