
## Input
```javascript input
const _ = require('lodash');

const input = [1, 2];
const result = _.every(input, (value) => value === 1, 'anotherArgument');
```

## Configuration
```json configuration
{
  "extension": "js"
}
```
